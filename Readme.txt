Prime numbers can be found in file "primeNumbers.rsc"

Key Generation only finds e value for large primes. It is fully working for small primes and will save the resulting keys to a file. So please use small primes to test functionality (around 1000 or less).

